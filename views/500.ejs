<!DOCTYPE html>
<html lang="id" class="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>500 - Duh, Server Lagi Ngambek | NirKyy</title>
    <link rel="icon" href="https://nirkyy.koyeb.app/favicon.ico" type="image/x-icon">
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700;900&display=swap" rel="stylesheet">
    <style type="text/tailwindcss">
        @tailwind base;
        @tailwind components;
        @tailwind utilities;

        body {
            font-family: 'Poppins', sans-serif;
            background-color: #030712; 
            color: #e5e7eb; 
            display: flex;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            overflow: hidden;
            text-align: center;
        }
        .error-container {
            position: relative;
            z-index: 10;
            padding: 2rem;
        }
        .error-code {
            font-size: clamp(6rem, 25vw, 15rem);
            font-weight: 900;
            line-height: 1;
            color: #1f2937; 
            text-shadow: 
                -1px -1px 0 #4b5563,
                 1px -1px 0 #4b5563,
                -1px  1px 0 #4b5563,
                 1px  1px 0 #4b5563,
                 -2px -2px 0 #374151,
                  2px -2px 0 #374151,
                 -2px  2px 0 #374151,
                  2px  2px 0 #374151,
                 -3px -3px 5px rgba(0,0,0,0.2);
            animation: pulse-error 3s infinite ease-in-out;
        }
       @keyframes pulse-error {
            0%, 100% { transform: scale(1); text-shadow: -1px -1px 0 #4b5563, 1px -1px 0 #4b5563, -1px 1px 0 #4b5563, 1px 1px 0 #4b5563, -2px -2px 0 #374151, 2px -2px 0 #374151, -2px 2px 0 #374151, 2px 2px 0 #374151, -3px -3px 5px rgba(0,0,0,0.2), 0px 0px 0px #ef4444; } /* red-500 */
            50% { transform: scale(1.02); text-shadow: -1px -1px 0 #4b5563, 1px -1px 0 #4b5563, -1px 1px 0 #4b5563, 1px 1px 0 #4b5563, -2px -2px 0 #374151, 2px -2px 0 #374151, -2px 2px 0 #374151, 2px 2px 0 #374151, -3px -3px 5px rgba(0,0,0,0.2), 0 0 15px #ef4444, 0 0 5px #ef4444; }
        }

        .vanta-bg-container {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: 1;
            filter: brightness(0.6) saturate(0.7);
        }
    </style>
</head>
<body>
    <div id="vanta-bg-500" class="vanta-bg-container"></div>
    <div class="error-container">
        <div id="error-code-text" class="error-code">500</div>
        <h1 class="mt-2 text-3xl font-bold tracking-tight text-white sm:text-5xl">Aduh, Server Lagi Istirahat!</h1>
        <p class="mt-4 text-base text-gray-400 sm:mt-6">Kayaknya ada yang gak beres di sisi server kami. Tim teknis lagi ngecek, coba lagi beberapa saat ya.</p>
        <div class="mt-10 flex flex-col sm:flex-row justify-center items-center gap-4">
            <a href="/"
               class="inline-block rounded-lg bg-blue-600 px-5 py-3 text-sm font-semibold text-white shadow-sm hover:bg-blue-500 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-blue-600 transition-transform duration-200 ease-in-out hover:scale-105 active:scale-95">
                <i class="fas fa-home mr-2"></i>Balik ke Beranda
            </a>
            <button onclick="window.location.reload();"
               class="inline-block rounded-lg bg-gray-700 px-5 py-3 text-sm font-semibold text-white shadow-sm hover:bg-gray-600 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-gray-700 transition-transform duration-200 ease-in-out hover:scale-105 active:scale-95">
                <i class="fas fa-sync-alt mr-2"></i>Coba Muat Ulang
            </button>
        </div>
    </div>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/three.js/r134/three.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/vanta@latest/dist/vanta.fog.min.js"></script> 
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/gsap.min.js"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" integrity="sha512-DTOQO9RWCH3ppGqcWaEA1BIZOC6xxalwEsw9c2QQeAIftl+Vegovlnee1c9QX4TctnWMn13TZye+giMm8e2LwA==" crossorigin="anonymous" referrerpolicy="no-referrer" />

    <script>
        document.addEventListener('DOMContentLoaded', () => {
            if (typeof VANTA !== 'undefined' && VANTA.FOG) {
                VANTA.FOG({
                    el: "#vanta-bg-500",
                    mouseControls: true,
                    touchControls: true,
                    gyroControls: false,
                    minHeight: 200.00,
                    minWidth: 200.00,
                    highlightColor: 0xef4444, // merah
                    midtoneColor: 0x3b82f6,   // biru
                    lowlightColor: 0x1e3a8a,  // biru tua
                    baseColor: 0x030712,      // bg-gray-950
                    blurFactor: 0.60,
                    speed: 1.20,
                    zoom: 0.80
                });
            }

            const errorCodeText = document.getElementById('error-code-text');
            if (errorCodeText) {
                gsap.fromTo(errorCodeText, 
                    { scale: 0.8, opacity: 0 }, 
                    { scale: 1, opacity: 1, duration: 0.8, ease: "elastic.out(1, 0.5)", delay: 0.2 }
                );
            }
            gsap.from(".error-container > *:not(.error-code)", 
                { y: 30, opacity: 0, stagger: 0.15, duration: 0.7, ease: "power2.out", delay: 0.5 }
            );
        });
    </script>
</body>
</html>
